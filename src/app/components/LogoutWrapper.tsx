'use client';

import Logout from './Logout';

const LogoutWrapper = () => {
  return (
    <div className='absolute top-4 right-4'>
      <Logout />
    </div>
  );
};

export default LogoutWrapper;
